// import { api, renderDOM } from '../modules';
import { LoginForm } from '../components';
async function login() {
    const loginForm = new LoginForm();
    // renderDOM(loginComponent.render());
}
export default login;
